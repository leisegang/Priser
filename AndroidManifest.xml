<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.is206.olpriser"
    android:versionCode="1"
    android:versionName="1.1" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="20" />

    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:configChanges="keyboardHidden|orientation"
        android:icon="@drawable/ikon"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo.Light.NoActionBar" >
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name" 
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Screen Activity -->
        <activity
            android:name=".AllUtestederActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Holo.Light.DarkActionBar" 
            android:screenOrientation="portrait" >
        </activity>

        <!-- Om Oss Activity -->
        <activity
            android:name=".AboutUsActivity"
            android:icon="@drawable/back"
            android:label="@string/omoss"
            android:theme="@android:style/Theme.Holo.Light.DarkActionBar"
            android:screenOrientation="portrait"  >
        </activity>

        <!-- Add Utested Activity -->
        <activity
            android:name=".NewUtestedActivity"
            android:icon="@drawable/back"
            android:label="@string/legg_til_utested"
            android:theme="@android:style/Theme.Holo.Light.DarkActionBar"
            android:screenOrientation="portrait"  >
        </activity>

        <!-- Edit Utested Activity -->
        <activity
            android:name=".EditUtestedActivity"
            android:icon="@drawable/back"
            android:label="@string/utested"
            android:theme="@android:style/Theme.Holo.Light.DarkActionBar"
            android:screenOrientation="portrait"  >
        </activity>
        
        <!-- Tips oss Activity -->
        <activity
            android:name=".TipsUsActivity"
            android:icon="@drawable/back"
            android:label="@string/tips"
            android:theme="@android:style/Theme.Holo.Light.DarkActionBar"
            android:screenOrientation="portrait"  >
        </activity>
    </application>

</manifest>
